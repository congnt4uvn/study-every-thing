
Dưới đây là tóm tắt ngắn gọn về **Spring Kafka advanced: Batch listeners với `@KafkaListener` và `List<ConsumerRecord>`**, kèm ví dụ code và input/output minh họa.

---

## 1. Khái niệm chính

- **Batch listener**: Thay vì xử lý từng message một, Spring Kafka cho phép bạn nhận **một danh sách các bản ghi** (`List<ConsumerRecord<K, V>>`) trong một lần gọi listener.
- Mục đích:
  - Tăng throughput (hiệu năng) khi xử lý nhiều message cùng lúc.
  - Giảm overhead do gọi listener nhiều lần.
- Cấu hình cần:
  - Bật chế độ batch cho `ConcurrentKafkaListenerContainerFactory`.
  - Dùng `@KafkaListener` với tham số kiểu `List<ConsumerRecord<K, V>>` hoặc `List<YourType>`.

---

## 2. Cấu hình `ConcurrentKafkaListenerContainerFactory` cho batch

```java
@Configuration
@EnableKafka
public class KafkaConfig {

    @Bean
    public ConsumerFactory<String, String> consumerFactory() {
        Map<String, Object> props = new HashMap<>();
        props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, "localhost:9092");
        props.put(ConsumerConfig.GROUP_ID_CONFIG, "batch-group");
        props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);
        props.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);

        // Một số cấu hình batch quan trọng
        props.put(ConsumerConfig.MAX_POLL_RECORDS_CONFIG, 50); // tối đa 50 record mỗi lần poll

        return new DefaultKafkaConsumerFactory<>(props);
    }

    @Bean
    public ConcurrentKafkaListenerContainerFactory<String, String> batchFactory() {
        ConcurrentKafkaListenerContainerFactory<String, String> factory =
                new ConcurrentKafkaListenerContainerFactory<>();
        factory.setConsumerFactory(consumerFactory());

        // Bật chế độ batch
        factory.setBatchListener(true);

        // (tuỳ chọn) xử lý thủ công offset
        factory.getContainerProperties().setAckMode(ContainerProperties.AckMode.MANUAL);

        return factory;
    }
}
```

---

## 3. Listener dạng batch với `List<ConsumerRecord>`

### 3.1. Ví dụ listener cơ bản

```java
@Service
public class BatchKafkaListener {

    @KafkaListener(
        topics = "orders",
        containerFactory = "batchFactory"
    )
    public void listen(List<ConsumerRecord<String, String>> records,
                       Acknowledgment ack) {

        System.out.println("Received batch size: " + records.size());

        for (ConsumerRecord<String, String> record : records) {
            System.out.printf("Partition: %d, Offset: %d, Key: %s, Value: %s%n",
                    record.partition(),
                    record.offset(),
                    record.key(),
                    record.value());
            // TODO: xử lý business logic
        }

        // Xác nhận offset sau khi xử lý xong cả batch
        ack.acknowledge();
    }
}
```

### 3.2. Input/Output minh họa

**Input (Kafka messages trong topic `orders`):**

| Partition | Offset | Key      | Value                          |
|----------:|--------|----------|--------------------------------|
| 0         | 10     | order-1  | {"id":1,"amount":100}          |
| 0         | 11     | order-2  | {"id":2,"amount":200}          |
| 1         | 5      | order-3  | {"id":3,"amount":300}          |

**Output (log từ listener):**

```text
Received batch size: 3
Partition: 0, Offset: 10, Key: order-1, Value: {"id":1,"amount":100}
Partition: 0, Offset: 11, Key: order-2, Value: {"id":2,"amount":200}
Partition: 1, Offset: 5,  Key: order-3, Value: {"id":3,"amount":300}
```

---

## 4. Listener batch với kiểu dữ liệu đã deserialize (`List<YourType>`)

Nếu bạn dùng `JsonDeserializer` và muốn nhận trực tiếp list object:

```java
public class Order {
    private Long id;
    private Double amount;
    // getters/setters
}
```

### Cấu hình ConsumerFactory với JSON

```java
@Bean
public ConsumerFactory<String, Order> orderConsumerFactory() {
    Map<String, Object> props = new HashMap<>();
    props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, "localhost:9092");
    props.put(ConsumerConfig.GROUP_ID_CONFIG, "order-batch-group");
    props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);
    props.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, JsonDeserializer.class);
    props.put(JsonDeserializer.TRUSTED_PACKAGES, "*");

    return new DefaultKafkaConsumerFactory<>(
            props,
            new StringDeserializer(),
            new JsonDeserializer<>(Order.class)
    );
}

@Bean
public ConcurrentKafkaListenerContainerFactory<String, Order> orderBatchFactory() {
    ConcurrentKafkaListenerContainerFactory<String, Order> factory =
            new ConcurrentKafkaListenerContainerFactory<>();
    factory.setConsumerFactory(orderConsumerFactory());
    factory.setBatchListener(true);
    return factory;
}
```

### Listener nhận `List<Order>`

```java
@Service
public class OrderBatchListener {

    @KafkaListener(
        topics = "orders",
        containerFactory = "orderBatchFactory"
    )
    public void listenOrders(List<Order> orders) {
        System.out.println("Received orders batch size: " + orders.size());
        for (Order order : orders) {
            System.out.println("Order id=" + order.getId()
                    + ", amount=" + order.getAmount());
            // xử lý logic
        }
    }
}
```

**Input (messages trong topic `orders`):**

```json
{"id": 1, "amount": 100.0}
{"id": 2, "amount": 200.5}
{"id": 3, "amount": 300.0}
```

**Output (log):**

```text
Received orders batch size: 3
Order id=1, amount=100.0
Order id=2, amount=200.5
Order id=3, amount=300.0
```

---

## 5. Một số điểm cần lưu ý

- `MAX_POLL_RECORDS_CONFIG` quyết định số record tối đa trong một batch.
- Batch listener thường đi kèm:
  - Xử lý offset thủ công (`AckMode.MANUAL` hoặc `MANUAL_IMMEDIATE`).
  - Cân nhắc retry / error handling cho từng record hoặc cả batch.
- Batch phù hợp khi:
  - Xử lý theo lô (ví dụ: ghi DB theo batch, gửi sang hệ thống khác theo lô).
  - Chấp nhận độ trễ cao hơn một chút để đổi lấy throughput cao hơn.

---

Nếu bạn cho thêm nội dung chi tiết (config hiện tại, use case cụ thể), tôi có thể viết lại ví dụ sát hơn với hệ thống của bạn.

---

[Next: doc_091.md](doc_091.md)



