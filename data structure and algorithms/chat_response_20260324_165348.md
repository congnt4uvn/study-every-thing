Sure! Below is a simple implementation of a singly linked list in Java. This implementation includes basic operations such as insertion, deletion, searching, and displaying the list.

```java
class SinglyLinkedList {
    // Node class representing each element in the list
    private static class Node {
        int data;
        Node next;

        Node(int data) {
            this.data = data;
            this.next = null;
        }
    }

    private Node head;

    // Insert a new node at the end of the list
    public void insert(int data) {
        Node newNode = new Node(data);
        if (head == null) {
            head = newNode;
        } else {
            Node current = head;
            while (current.next != null) {
                current = current.next;
            }
            current.next = newNode;
        }
    }

    // Delete a node with a specific value
    public void delete(int data) {
        if (head == null) {
            System.out.println("List is empty.");
            return;
        }

        if (head.data == data) {
            head = head.next;
            return;
        }

        Node current = head;
        while (current.next != null && current.next.data != data) {
            current = current.next;
        }

        if (current.next == null) {
            System.out.println("Element not found.");
        } else {
            current.next = current.next.next;
        }
    }

    // Search for a node with a specific value
    public boolean search(int data) {
        Node current = head;
        while (current != null) {
            if (current.data == data) {
                return true;
            }
            current = current.next;
        }
        return false;
    }

    // Display the list
    public void display() {
        if (head == null) {
            System.out.println("List is empty.");
            return;
        }

        Node current = head;
        while (current != null) {
            System.out.print(current.data + " -> ");
            current = current.next;
        }
        System.out.println("null");
    }

    // Main method to demonstrate the linked list operations
    public static void main(String[] args) {
        SinglyLinkedList list = new SinglyLinkedList();

        // Insert elements
        list.insert(10);
        list.insert(20);
        list.insert(30);
        list.insert(40);

        // Display the list
        System.out.println("Linked List:");
        list.display();

        // Search for an element
        System.out.println("Search for 20: " + list.search(20));
        System.out.println("Search for 50: " + list.search(50));

        // Delete an element
        list.delete(20);
        System.out.println("After deleting 20:");
        list.display();

        // Try to delete an element not in the list
        list.delete(50);
    }
}
```

### Explanation:

- **Node Class**: Represents each element in the linked list. It contains data and a reference to the next node.
- **Insert Method**: Adds a new node with the specified data at the end of the list.
- **Delete Method**: Removes the first occurrence of a node with the specified data.
- **Search Method**: Checks if a node with the specified data exists in the list.
- **Display Method**: Prints all the elements in the list.
- **Main Method**: Demonstrates the usage of the linked list by performing various operations and printing the results.

You can run this code in a Java environment to see how the singly linked list works.